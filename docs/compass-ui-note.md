# Compass UI behavior

Users may select one or more housing models directly. Without a direct selection, the initial screen uses management preference and primary goal to suggest a starting category. This is intentionally a screening suggestion, not an investment recommendation. Capital is captured only as a signal in the first version and is not yet persisted.